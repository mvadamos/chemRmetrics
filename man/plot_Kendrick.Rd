% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_Kendrick.R
\name{plot_Kendrick}
\alias{plot_Kendrick}
\title{Create Kendrick Mass Defect and Kendrick Mass Remainder Plots for MALDI-MS Mass Spectra}
\usage{
plot_Kendrick(
  df,
  unit_mass,
  unit_name,
  SNR,
  scale,
  trunc = FALSE,
  max_mass,
  method = "MAD",
  halfWindowSize = 10,
  peak_pick = "auto",
  ymin,
  ymax
)
}
\arguments{
\item{df}{Dataframe containing MALDI-MS mass spectra data}

\item{unit_mass}{Mass of repeating unit for Kendrick plots}

\item{unit_name}{Name of repeating unit for Kendrick plots}

\item{SNR}{Minimum signal-to-noise ratio used function when performing peak-picking.
If peak_pick = 'manual', 'SNR' refers to minimum normalised value (values are normalised
to unity then multiplied by 'scale')}

\item{scale}{Value used to adjust the size of data points in Kendrick plot (scale will also
affect the SNR value when peak_pick = 'manual')}

\item{trunc}{Used to designate if the user would like to reduce the mass range of
the spectra (Default = FALSE)}

\item{max_mass}{The maximum mass desired for the mass spectra which will be used to
truncate the mass range if 'trunc' = TRUE}

\item{method}{The peak picking method used for the 'detectPeaks()' function in the
MALDIquant package (see MALDIquant package guide for further details)}

\item{halfWindowSize}{The halfwindow size used during the peak picking (see MALDIquant
package guide for further details)}

\item{peak_pick}{Can be set to 'auto' or 'manual'. When setting to 'auto' peak picking
is performed using the 'detectPeaks()' function within the MALDIquant package. Use 'manual'
if m/z value are nominal, all masses with a normalised intensity values greater than 'SNR'
will be included as a peak}

\item{ymin}{Minimum y-value for KMD and KMR PDF plots}

\item{ymax}{Maximum y-value for KMD and KMR PDF plots}
}
\value{
A folder labelled 'KMD Plots' in which all Kendrick mass defect plots will be output

A folder labelled 'KMR Plots' in which all Kendrick mass remainder plots will be output

A kendrick mass defect plot as a '.pdf' file for each mass spectrum in 'df'

A kendrick mass remainder plot as a '.pdf' file for each mass spectrum in 'df'

A kendrick mass defect plot as a '.html' file for each mass spectrum in 'df'

A kendrick mass remainder plot as a '.html' file for each mass spectrum in 'df'

All data for each kendrick mass defect plot in '.csv' format

All data for each kendrick mass remainder plot in '.csv' format
}
\description{
This function creates Kednrick mass defect and Kendrick mass remainder plots for
MALDI-MS mass spectra stored in a dataframe. This function assumes that the first
'n' columns of the dataframe contain non-numeric variables with the remainder
containing mass spectra data with each row corresponding to a different sample.
Data imported using the 'load_MALDI()' function will already be in the correct format.
This function creates two folders in the working directory in which the Kendrick plots
will be exported.
}
